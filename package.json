{
  "name": "angelus-desktop",
  "private": true,
  "version": "0.5.0-rc.1",
  "description": "Tauri desktop shell for the Angelus local Agent workbench",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:backend": "node scripts/build-backend.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4"
  },
  "dependencies": {
    "dompurify": "3.2.6",
    "marked": "15.0.12"
  }
}
